API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_USE_DUMMY_CATALOG=false
# Optional: comma-separated image URLs aligned with dummy products order.
# Recommended to use Cloudflare Images delivery URLs for speed and global caching.
# NEXT_PUBLIC_DUMMY_IMAGE_URLS=https://imagedelivery.net/<hash>/product-01/public,https://imagedelivery.net/<hash>/product-02/public
